stages:
- name: Build Docker Image
  steps:
  - runScriptConfig:
      image: node:latest
      shellScript: |-
        npm i
        npm run test
        npm run build
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: pokedex
      registry: 10.43.35.128
  when:
    branch:
      include:
      - master
timeout: 60
notification: {}
